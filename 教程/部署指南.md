# 博客部署指南 - 发布到互联网

## 介绍

部署就是把你的博客放到互联网上，让全世界的人都能访问。本指南将介绍几种免费的部署方案。

## 方案一：GitHub Pages（推荐）

GitHub Pages 是 GitHub 提供的免费静态网站托管服务。

### 优点
- 完全免费
- 稳定可靠
- 自动 HTTPS
- 配置简单

### 步骤

#### 1. 注册 GitHub 账号

1. 访问 [GitHub](https://github.com/)
2. 点击 "Sign up"（注册）
3. 填写用户名、邮箱、密码
4. 验证邮箱
5. 完成注册

**重要：** 记住你的用户名，后面会用到！

#### 2. 创建仓库

1. 登录 GitHub
2. 点击右上角的 "+" 号，选择 "New repository"（新建仓库）
3. 仓库名称**必须**是：`你的用户名.github.io`
   - 例如：如果你的用户名是 `zhangsan`，仓库名就是 `zhangsan.github.io`
4. 选择 "Public"（公开）
5. 勾选 "Add a README file"
6. 点击 "Create repository"（创建仓库）

#### 3. 配置 Git

第一次使用 Git 需要配置用户信息。打开命令行，输入：

```bash
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"
```

例如：
```bash
git config --global user.name "zhangsan"
git config --global user.email "zhangsan@example.com"
```

#### 4. 部署 HTML 博客

如果你用的是 HTML 方式搭建博客：

**方法 1：通过网页上传**

1. 在 GitHub 仓库页面，点击 "Add file" → "Upload files"
2. 把你的所有文件拖进去（index.html, css, js 等）
3. 在下方填写提交信息，如 "上传博客文件"
4. 点击 "Commit changes"
5. 等待几分钟，访问 `https://你的用户名.github.io`

**方法 2：使用 Git 命令**

```bash
# 在你的博客文件夹中
git init
git add .
git commit -m "第一次提交"
git branch -M main
git remote add origin https://github.com/你的用户名/你的用户名.github.io.git
git push -u origin main
```

#### 5. 部署 Hexo 博客

如果你用的是 Hexo：

**步骤 1：安装部署插件**

```bash
npm install hexo-deployer-git --save
```

**步骤 2：修改配置**

打开 `_config.yml`，在最下面添加：

```yaml
# 部署配置
deploy:
  type: git
  repo: https://github.com/你的用户名/你的用户名.github.io.git
  branch: main
```

例如：
```yaml
deploy:
  type: git
  repo: https://github.com/zhangsan/zhangsan.github.io.git
  branch: main
```

**步骤 3：生成并部署**

```bash
hexo clean    # 清理缓存
hexo g        # 生成静态文件
hexo d        # 部署
```

或者一条命令：
```bash
hexo clean && hexo g -d
```

第一次部署时，会要求输入 GitHub 用户名和密码（或 token）。

**步骤 4：访问你的博客**

打开浏览器，访问 `https://你的用户名.github.io`

恭喜！你的博客已经上线了！🎉

### 更新博客

以后每次更新：

**HTML 博客：**
- 修改文件
- 重新上传到 GitHub（或用 git push）

**Hexo 博客：**
```bash
hexo clean && hexo g -d
```

## 方案二：Netlify

Netlify 是另一个优秀的免费托管平台，支持自定义域名。

### 步骤

#### 1. 注册 Netlify

1. 访问 [Netlify](https://www.netlify.com/)
2. 点击 "Sign up"
3. 可以用 GitHub 账号登录

#### 2. 部署网站

**方法 1：拖放部署（最简单）**

1. 登录后，进入控制台
2. 直接把你的博客文件夹拖到页面中央的 "Drop zone"
3. 等待部署完成
4. 会自动生成一个网址，如 `random-name-123.netlify.app`

**方法 2：连接 GitHub**

1. 点击 "New site from Git"
2. 选择 "GitHub"
3. 授权 Netlify 访问你的仓库
4. 选择你的博客仓库
5. 如果是 Hexo 博客，配置：
   - Build command: `hexo generate`
   - Publish directory: `public`
6. 点击 "Deploy site"

#### 3. 自定义域名（可选）

1. 在站点设置中，找到 "Domain management"
2. 点击 "Add custom domain"
3. 输入你的域名
4. 按照提示设置 DNS

## 方案三：Vercel

Vercel 速度快，部署简单，也支持自定义域名。

### 步骤

1. 访问 [Vercel](https://vercel.com/)
2. 用 GitHub 账号登录
3. 点击 "New Project"
4. 选择你的博客仓库
5. 如果是 Hexo：
   - Framework Preset: 选择 "Hexo"
   - 其他保持默认
6. 点击 "Deploy"
7. 等待部署完成

## 自定义域名

如果你想要自己的域名（如 www.你的名字.com），需要购买域名。

### 购买域名

**国内平台：**
- 阿里云（万网）
- 腾讯云
- 西部数码

**国外平台：**
- Namecheap（推荐，便宜）
- GoDaddy
- Google Domains

**价格：** 通常 .com 域名每年 50-100 元

### 配置域名

#### GitHub Pages

1. 在你的仓库根目录创建 `CNAME` 文件
2. 文件内容只有一行：`www.你的域名.com`
3. 在域名服务商那里添加 DNS 记录：
   - 类型：CNAME
   - 主机记录：www
   - 记录值：你的用户名.github.io

#### Netlify / Vercel

在控制面板中找到域名设置，按照提示操作即可，通常更简单。

## 配置 HTTPS

### GitHub Pages

GitHub Pages 会自动配置 HTTPS，无需手动设置。在仓库设置中确保启用了 "Enforce HTTPS"。

### Netlify / Vercel

这些平台会自动提供免费的 SSL 证书，自动启用 HTTPS。

## 性能优化

### 1. 压缩图片

使用工具压缩图片：
- [TinyPNG](https://tinypng.com/)
- [Squoosh](https://squoosh.app/)

### 2. 使用 CDN（Hexo）

修改 `_config.yml`：

```yaml
# 使用 CDN 加速
cdn:
  enable: true
  jquery: https://cdn.jsdelivr.net/npm/jquery@3/dist/jquery.min.js
```

### 3. 开启缓存

大多数托管平台会自动配置缓存，无需手动设置。

## SEO 优化

### 1. 提交到搜索引擎

**百度：**
1. 访问 [百度搜索资源平台](https://ziyuan.baidu.com/)
2. 添加网站
3. 验证所有权
4. 提交 sitemap

**Google：**
1. 访问 [Google Search Console](https://search.google.com/search-console)
2. 添加网站
3. 验证所有权
4. 提交 sitemap

### 2. 生成 sitemap（Hexo）

安装插件：
```bash
npm install hexo-generator-sitemap --save
npm install hexo-generator-baidu-sitemap --save
```

配置 `_config.yml`：
```yaml
sitemap:
  path: sitemap.xml

baidusitemap:
  path: baidusitemap.xml
```

### 3. 优化文章

- 使用有意义的标题
- 添加合适的关键词
- 写高质量的原创内容
- 定期更新

## 备份

定期备份你的博客内容：

### GitHub 自动备份

如果使用 GitHub，你的代码已经在云端备份了。但建议：
1. 定期克隆到本地
2. 在多个地方保存

### 本地备份

```bash
# 备份整个博客文件夹
cp -r my-blog my-blog-backup-2024-01-15
```

或者使用 Git：
```bash
git clone https://github.com/你的用户名/你的仓库.git
```

## 常见问题

### 1. 部署后 404 错误

**可能原因：**
- 仓库名不正确（必须是 `用户名.github.io`）
- 文件没有上传成功
- GitHub Pages 没有启用

**解决方法：**
- 检查仓库设置中的 GitHub Pages 配置
- 确保 `index.html` 在根目录

### 2. 样式/图片显示不正常

**可能原因：**
- 路径错误（使用了绝对路径）

**解决方法：**
- 使用相对路径
- Hexo 用户检查 `_config.yml` 中的 `url` 和 `root` 配置

### 3. 更新后看不到变化

**解决方法：**
- 清除浏览器缓存（Ctrl+F5）
- GitHub Pages 可能需要等待几分钟
- Hexo 用户执行 `hexo clean` 后重新部署

### 4. Git 推送失败

**可能原因：**
- 没有权限
- 网络问题

**解决方法：**
- 使用 GitHub Token 代替密码
- 检查网络连接
- 使用 SSH 方式（更安全）

## 监控网站

### Google Analytics（谷歌分析）

1. 注册 [Google Analytics](https://analytics.google.com/)
2. 创建媒体资源
3. 获取跟踪 ID
4. 添加到网站：
   - HTML 博客：在 `<head>` 中添加跟踪代码
   - Hexo：在主题配置中添加 tracking ID

### 百度统计

1. 注册 [百度统计](https://tongji.baidu.com/)
2. 添加网站
3. 获取统计代码
4. 添加到网站

## 持续集成（进阶）

使用 GitHub Actions 自动部署（Hexo）：

在仓库中创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy Blog

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'
      
      - name: Install Dependencies
        run: npm install
      
      - name: Build
        run: npm run build
      
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
```

这样每次推送代码，就会自动部署！

## 下一步

现在你的博客已经成功部署到互联网上了！接下来：

1. 开始写作，持续更新
2. 分享你的博客链接
3. 在社交媒体推广
4. 不断优化和改进

## 有用的链接

- [GitHub Pages 文档](https://docs.github.com/cn/pages)
- [Netlify 文档](https://docs.netlify.com/)
- [Vercel 文档](https://vercel.com/docs)
- [Hexo 部署文档](https://hexo.io/zh-cn/docs/deployment)

祝你博客运营顺利！🚀
